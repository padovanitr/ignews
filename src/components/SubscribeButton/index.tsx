import styles from './styles.module.scss';

export function SubsripbeButton(){
    return (
        <button
            type="button"
            className={styles.subscribeButton}
        >
            Subscribe now
        </button>
    )
}